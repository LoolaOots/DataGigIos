// Config.xcconfig
// Local development configuration.
// Reference this file in the Xcode project's Build Settings → Configuration Settings File.
// Then add BACKEND_BASE_URL to the Info.plist as $(BACKEND_BASE_URL).

// BACKEND_BASE_URL = http:$()//localhost:8000
BACKEND_BASE_URL = http:$()/192.168.86.27:8000
